# CourtVision — direct runtime dependencies only.
# Test-only dependencies (selenium, pytest, webdriver-manager) are added in
# Phase 3, the phase that actually uses them.

Flask==3.1.3
requests==2.34.2
python-dotenv==1.2.2
